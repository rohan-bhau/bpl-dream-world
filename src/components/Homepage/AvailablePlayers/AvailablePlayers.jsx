import React from "react";

import Card from "../../ui/Card";

const AvailablePlayers = ({ playerData }) => {
    // console.log(playerData);

    // console.log(PlayerName)
    
  return <Card playerData={playerData}></Card>;
};

export default AvailablePlayers;
